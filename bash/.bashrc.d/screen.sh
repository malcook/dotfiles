# Attaches to a screen session. If the session is attached elsewhere,
# detaches that other display. If no session exists, creates one. If
# multiple sessions exist, uses the first one.)
alias sdrr='screen -dRR'
